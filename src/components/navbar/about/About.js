import React from "react";
import Button from "../../buttons/button/Button";

const About = (props) => {
  return (
    <div>
      <Button name={props.about} />
    </div>
  );
};

export default About;
