import React from "react";
import Button from "../../buttons/button/Button";

const Home = (props) => {
  return (
    <div>
      <Button name={props.home} />
    </div>
  );
};

export default Home;
